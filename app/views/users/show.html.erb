<p id="notice"><%= notice %></p>

<h2 style="text-align:center">User Profile</h2>

<div class="card">
  <% if @user.avatar.present? %>
    <img src="<%= @user.avatar.url %>" alt="<%= @user.name %>" style="width:100%">
  <% end %>
  <h1><%= @user.name %> </h1>
  <p class="title"><%= @user.address.values.join(', ') if @user.address.present? %></p>
  <%= link_to 'Edit', edit_user_path(@user) %>
  <%
=begin%>
 <%= link_to 'Back', users_path %> 
<%
=end%>
</div>
